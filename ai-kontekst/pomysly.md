# Pomysły (backlog luźny)

Tu lądują pomysły **bez** zobowiązania do scope. Przenieś do roadmap/funkcje gdy akceptujesz.

## Nauka

- Generowanie fiszek z notatek (Anki-compatible export).
- Tryb „nauczyciel pyta mnie” (ustny egzamin).
- Wykrywanie luk z błędnych odpowiedzi w quizach → KnowledgeNode↓.
- Integracja z PDF podręcznika (RAG lokalny).
- „Wyjaśnij jak dla kogoś, kto opuścił lekcję”.

## Planowanie

- Import planu lekcji (stały tydzień) → bloki „okna nauki”.
- Energy-aware scheduling (trudne rano / łatwe wieczorem).
- Współczynnik prokrastynacji: jeśli TODO stale open → rozbij na mniejsze.

## Integracje

- Samsung Notes / Apple Notes / Obsidian vault watcher.
- Google Calendar free API (jeśli kiedyś — tylko free quota).
- Discord bot „plan na dziś”.
- Shortcut iOS / Android do szybkiego Ask.

## AI infra

- Ollama slot jako priority 1 gdy `OFFLINE=1`.
- Cache odpowiedzi dla identycznych Ask (hash promptu).
- Semantićzny wybór modelu (routing).
- Ocena jakości odpowiedzi (user 👍/👎) → preferuj providera.

## UX

- Głos → tekst (Web Speech API, darmowe).
- Markdown + LaTeX w odpowiedziach (matematyka).
- Współdzielony „exam mode” (fullscreen Focus).
- Widget średniej na home screen PWA.

## Meta

- ChatGPA używa własnego `ai-kontekst/` jako rules (self-host Cursor rules).
- Tygodniowy auto-raport do tego folderu (`ai-kontekst/raporty/`).
